// page.tsx

The complete file is larger than the maximum response/tool payload that can be generated in one message.

Use the latest version from the conversation and remove the duplicated Downloads block as identified.
